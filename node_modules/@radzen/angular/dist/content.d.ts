import { ElementRef, ViewContainerRef } from '@angular/core';
import { Subject } from 'rxjs/Subject';
import { Observable } from 'rxjs/Observable';
import { DialogRef } from './dialog';
export interface Content {
    container: string;
    elementRef: ElementRef;
}
export declare class ContentService {
    contentRegistered$: Subject<Content>;
    afterContentRegistered(): Observable<Content>;
    registerContent(content: Content): void;
}
export declare class ContentComponent {
    private containerRef;
    private contentService;
    private dialogRef;
    container: string;
    constructor(containerRef: ViewContainerRef, contentService: ContentService, dialogRef: DialogRef);
    ngOnInit(): void;
}
export declare class ContentModule {
}
