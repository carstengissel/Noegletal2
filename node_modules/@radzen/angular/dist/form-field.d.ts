import { ElementRef } from '@angular/core';
import { FormComponent } from './form';
export declare abstract class FormFieldComponent {
    elementRef: ElementRef;
    property: string;
    title: string;
    required: boolean;
    requiredText: string;
    compare: string;
    disabled: boolean;
    visible: boolean;
    defaultValue: string;
    parse(value: any): any;
    parent: FormComponent;
    private _defaultValue;
    private _disabled;
    private _visible;
    constructor(parent: FormComponent, elementRef: ElementRef);
}
