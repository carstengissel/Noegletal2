import { OnChanges, SimpleChanges } from '@angular/core';
import { SelectItem } from 'primeng/api';
import { FormFieldComponent } from './form-field';
export declare class LookupFormFieldComponent extends FormFieldComponent implements OnChanges {
    textProperty: string;
    valueProperty: string;
    placeholder: string;
    data: any[];
    allowFiltering: boolean;
    allowClear: boolean;
    multiple: boolean;
    options: SelectItem[];
    ngOnChanges(changes: SimpleChanges): void;
    private setOptions();
}
